# .env.example — template of required environment variables.
#
# Copy this file to `.env` and fill in the real values. Real credentials live ONLY
# in `.env`, which is gitignored and must never be committed. Add one line per
# variable this client's automations require (API keys, tokens, base URLs, etc.).
#
# Example:
# API_BASE_URL=
# API_KEY=
