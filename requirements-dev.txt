# Development and test dependencies. NOT packaged into the Lambda.
-r requirements.txt

pytest>=8.0

# Renders the campaign report locally (no Chromium layer on a laptop). After
# installing: `playwright install chromium`. Not shipped to Lambda.
playwright>=1.63,<2
# tests/test_template.py reads infra/template.yaml. SAM CLI brings it anyway.
PyYAML>=6.0

# The Lambda runtime provides boto3; this is only for running the DynamoDB-backed
# idempotency store locally.
boto3>=1.34

# Deploys the stack in infra/template.yaml.
aws-sam-cli>=1.140
