# Runtime dependency for LIVE API calls. Dry-run and the test suite need only the
# Python standard library, so this is optional until you run against production.
requests>=2.31

# DynamoDB-backed idempotency. The Lambda runtime already ships boto3, so this is
# only needed to run the DynamoDB path locally; the file-backed store used in dev
# and tests needs nothing.
boto3>=1.34

# Dev / test
pytest>=8.0
