"""Automations for the Dror Barak workspace.

Each module exposes a ``run(...)`` function and a ``main()`` CLI entrypoint with
``--dry-run``. See ``CLAUDE.md`` for the catalogue and ``README.md`` for how to run
each one (scheduled / webhook / manual).
"""
